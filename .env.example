# 环境变量配置模板
# 复制为 .env 并填入实际值；或直接设置系统环境变量。
# 所有变量均有安全默认值，仅在非默认部署时需要修改。

# ── LLM 供应商 ────────────────────────────────────────────────
# 方式 A：OpenCode Go 套餐（推荐，免费额度内运行）
# 密钥从 ~/.local/share/opencode/auth.json 自动读取（opencode-go.key）
OPENAI_API_BASE=https://opencode.ai/zen/go/v1
# OPENAI_API_KEY 由 launch 脚本自动注入，无需手动设置

# 方式 B：DashScope（阿里云百炼）
# DASHSCOPE_API_KEY=sk-xxx

# ── 模型指定（覆盖路由器默认链）─────────────────────────────
# PILOT_LLM=openai/qwen3.7-plus
# PILOT_VLM=openai/deepseek-v4-flash-vision-exp

# ── Embedding ────────────────────────────────────────────────
# HF_HOME=D:\hf_cache

# ── 并发与超时 ────────────────────────────────────────────────
# P4_CONCURRENCY=3
# P4_T_INDEX=2700
# P4_T_RETRIEVE=900
# P4_T_GENERATE=480
